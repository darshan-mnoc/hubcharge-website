# components/curve-spark.tsx

- CurveSpark · function · L10-L49 — function CurveSpark({ model, className = "", tone = "light", }: { model: EvModel; className?: string; /** The curve line is ink on paper and white on navy — at 14% fill the * original single palette vanished entirely on the dark cards. */ tone?: "light" | "dark"; })
