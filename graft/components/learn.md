# components/learn.tsx

- GuideBreadcrumb · function · L7-L52 — function GuideBreadcrumb({ trail, }: { /** [label, href] pairs ending with the current page (href ignored for last) */ trail: [string, string][]; })
- GuideShort · function · L64-L89 — function GuideShort({ slug }: { slug: string })
- GuideCta · function · L92-L117 — function GuideCta({ headline = "Ready to try full-service charging?", sub = "Pull up, stay in your car, and let our team handle the rest — no app needed.", }: { headline?: string; sub?: string; })
- GuideFooter · function · L123-L165 — function GuideFooter({ slug }: { slug: string })
